# Netlify build config — read automatically when the repo is imported.
# With this file present, no manual build settings are needed in the Netlify UI.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Astro 5 needs a modern Node; pin it so builds are reproducible.
  NODE_VERSION = "20"
